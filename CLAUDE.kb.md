# ナレッジベース（kb/）

プロジェクトの実装詳細や設計判断など、正式ドキュメントに書くほどではないが記録し
ておきたい知見を `kb/` ディレクトリに保存する。

## いつ記録するか

- コードを読んだだけでは理解しづらい仕組みを発見した時
- 複数の機構が組み合わさって動作している場合の解説
- 「なぜこう実装したのか」という設計判断の記録
- 将来の自分や他の開発者が困った時のヒント
- バグ調査で得た知見や回避策
- セッション終了時

## ファイルの種類

- **raw KB**: セッション内で得た知見を情報の優劣に限らずため込む。将来整理されるのが前提の情報。
  - ファイル名: `kb/$(date +%Y%m%d%H%M%S).md` （例: `kb/20260210013318.md`）
- **refined KB**: 要約後、raw KB から集められた特定のトピックについて書かれた KB。
  - ファイル名: トピック名ベース（例: `kb/speaker-map.md`）

## 要約方法

- 全ての KB から特定のトピックについて情報を集め、"トピック名" + ".md" のファイルに情報を集約する。
- この時、知見は移動を基本とする。つまり、要約によって refined KB に書き込まれた知見は raw KB から削除する。これにより、知見が複数個所に散見されるのを防ぐことが出来る。

## バージョン管理

- KB を新規作成した場合は `svn add` でバージョン管理対象に追加しておく。
- commit は整理した直後に行う。これにより、知見の整理状態をリビジョンとして記録する。

## Claude くんの動作規則

1. **セッション開始時**に `CLAUDE.kb.md` を読み、KB の運用ルールを把握する。
2. 続けて以下コマンドを実行し、全KB ファイルの1行目（概要）を読んで把握しておく：
   ```
   awk 'FNR==1{print FILENAME ":" $0}' kb/*.md
   ```
3. 作業中に、把握した概要と関連する課題が発生したら、**自動で該当ファイルを読む**。
   - 例：環境変数に関連する作業なら、概要に「環境変数」とある KB ファイルを読む。
4. 読んだ知見を適切に活用し、同じ問題を再び調査する手間を省く。
